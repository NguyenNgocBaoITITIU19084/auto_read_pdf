# Đọc ảnh không cần Gemini & chỉnh sửa nhỏ (v2.1.0 — Giai đoạn D) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Máy Windows không có Gemini key vẫn đọc được ảnh booking (≥ 90% trường chính đúng); khớp voyage gần đúng có đánh dấu; sửa 5 hạn chế đã biết.

**Architecture:** OCR offline là một "engine" mới trong `image_extractor.run_local_ocr` (lazy import, singleton), đặt trước swift/tesseract; phương án engine được chọn bằng một script so sánh chạy trên ảnh thật. Khớp voyage gần đúng là lượt thứ hai trong `eport_client.search_vessels_detailed`, chỉ chấp nhận khi đúng **một** chuyến ứng viên; kết quả mang cờ `VOYAGE_MATCH`/`REQUESTED_VOYAGE`, lưu cột `vessel_schedules.requested_voyage` để UI hiện nhãn. 5 chỉnh sửa nhỏ là các thay đổi cục bộ backend + UI.

**Tech Stack:** FastAPI + SQLite, APScheduler 3.11, `rapidocr-onnxruntime` (dự kiến, chốt sau spike), React 18 + TypeScript, Vitest (đã thêm ở kế hoạch C Task 1 — nếu D chạy trước C, làm Step 5 của C Task 1 trước).

## Quyết định sản phẩm (cần anh/chị xác nhận trước khi làm)

1. **Khôi phục "Gộp" (mặc định):** bộ sưu tập trùng tên được gộp vào bộ sưu tập sẵn có (không tạo bản `_imported_…` nữa); dòng trùng thì **bản backup thắng** (tàu/cont theo khoá UNIQUE sẵn có, booking theo cặp `booking_no + pdf_name`, color rule theo `target_table + column_key + match_value + match_type`). **"Thay thế":** xoá toàn bộ bộ sưu tập (kèm booking/tàu/cont/watchlist) và color rules rồi nạp đúng nội dung backup; cài đặt hệ thống (Gemini key, auto-sync) giữ nguyên; có hộp xác nhận nguy hiểm.
2. **Chạy bù giờ cố định:** chỉ chạy bù khi mốc giờ gần nhất đã qua **sau** lần chạy cuối (`last_run_at` được lưu DB). Chưa từng chạy → coi là đã lỡ. Chế độ chu kỳ giữ nguyên hành vi chạy sau 30s.
3. **Tra lại container:** mặc định chỉ lưu sự kiện cùng `event_type` với các dòng đã chọn; thêm hành động hàng loạt riêng "Tra lại (mọi sự kiện)".
4. **Khớp voyage gần đúng:** chỉ tự nhận khi có đúng 1 chuyến ePort khớp gần đúng; ≥ 2 chuyến → vẫn báo "không khớp" kèm danh sách.
5. **Tra tàu nhanh:** không tự lưu; nút "Lưu vào tab Tàu" từng kết quả; "Theo dõi" đồng thời lưu kết quả đó. Hành động hàng loạt "Tra tàu" ở tab Booking vẫn lưu như cũ.

## Global Constraints

- OCR: máy Windows **không có key** đọc đúng **≥ 90% trường chính** trên bộ 10 ảnh booking thật. Trường chính: `Booking No`, `Carrier`, `Vessel`, `ETD`, `Port of Discharging`, `Equipment Type`, `Q'ty`, `Empty Pick Up CY`, `Full return CY`, `Port Cargo Cut-off` (10 trường × 10 ảnh = 100 ô, cần ≥ 90 ô đúng).
- Spike OCR tối đa **0.5 ngày**, so sánh đúng 3 phương án: `rapidocr`, `paddleocr`, Windows OCR API.
- Bộ cài tăng thêm do OCR: nếu > 100MB → báo lại trước khi tích hợp (phương án tải model lần đầu).
- OCR import lười: không làm chậm khởi động backend (đo `/health` sẵn sàng trước/sau chênh < 1s).
- Ảnh booking thật của khách **không commit vào git** (thư mục `scripts/ocr_spike/samples/` nằm trong `.gitignore`).
- API giữ tương thích ngược: `/vessels/search` mặc định vẫn lưu (`save=true`); `/restore` không có `mode` = `merge`; `/containers/resync` không có `all_events` = lọc theo sự kiện.
- Giờ lưu DB: `"%Y-%m-%d %H:%M:%S"` theo `Asia/Ho_Chi_Minh`.
- Chuỗi UI thêm vào `translations.ts` cả `vi` và `en`, chỉ thêm key trong section liên quan.
- Test: `pytest backend/tests tests -q`; `cd frontend && npm test && npm run build`.
- Commit message kết thúc bằng `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## File Structure

| File | Trách nhiệm |
|---|---|
| `backend/app/services/extractor.py` | `_fmt_date` kiểm tra ngày hợp lệ (D3e) |
| `backend/app/services/background_tasks.py` | Lưu `last_run_at`, `missed_times_slot`, chạy bù có điều kiện (D3b) |
| `backend/app/core/database.py` | `import_backup_data(mode)` (D3a); cột `requested_voyage` (D2) |
| `backend/app/api/export_backup.py` | `POST /restore?mode=` (D3a) |
| `backend/app/api/vessels.py`, `schemas/models.py` | `save` flag, `POST /vessels/save`, trả `match` (D3c, D2) |
| `backend/app/api/containers.py` | `all_events` khi tra lại (D3d) |
| `backend/app/services/eport_client.py` | `voyage_cores`, khớp gần đúng (D2) |
| `backend/app/services/ocr_layout.py` (mới) | Ghép box OCR thành dòng văn bản (D1) |
| `backend/app/services/image_extractor.py` | Engine `rapidocr` (D1) |
| `scripts/ocr_spike/compare_ocr.py` (mới) | So sánh 3 engine trên ảnh thật (D1) |
| `backend_app.spec`, `requirements.txt`, `backend/requirements.txt` | Đóng gói OCR (D1) |
| `frontend/src/components/common/BackupModal.tsx` | Chọn Gộp/Thay thế |
| `frontend/src/components/booking/QuickVesselSearch.tsx` | Lưu thủ công |
| `frontend/src/components/container/ContainerTab.tsx` | Hành động "Tra lại (mọi sự kiện)" |
| `frontend/src/components/vessel/VesselRow.tsx`, `VesselDetailModal.tsx` | Nhãn "Khớp gần đúng" |
| `frontend/src/services/api.ts`, `types/index.ts`, `i18n/translations.ts` | Hàm API, kiểu, chuỗi |

---

### Task 1: Kiểm tra ngày hợp lệ trước khi đổi định dạng (D3e)

**Files:**
- Modify: `backend/app/services/extractor.py:16-57`
- Test: `tests/test_extractor.py`

**Interfaces:**
- Produces: `_fmt_date(day, month, year, time_part) -> Optional[str]` (None khi ngày/giờ không tồn tại); `parse_date_str` trả chuỗi gốc (đã strip) nếu không hợp lệ.

- [ ] **Step 1: Viết test thất bại** — thêm vào `tests/test_extractor.py`:
```python
import pytest
from backend.app.services.extractor import parse_date_str, parse_etd


@pytest.mark.parametrize("raw", ["2026-02-30", "30/02/2026", "31Apr26", "2026-13-01", "15/09/2026 25:00", "00/09/2026"])
def test_parse_date_str_keeps_invalid_dates_unchanged(raw):
    assert parse_date_str(f"  {raw} ") == raw


@pytest.mark.parametrize("raw,expected", [
    ("29Feb28", "29/02/2028"),
    ("2026-09-15 23:59", "15/09/2026 23:59"),
    ("1/9/2026", "01/09/2026"),
])
def test_parse_date_str_valid_dates_still_normalized(raw, expected):
    assert parse_date_str(raw) == expected


def test_parse_etd_with_invalid_etd_keeps_raw():
    assert parse_etd("13Jul26/31Jun26") == "31Jun26"
```

- [ ] **Step 2: Chạy, xác nhận thất bại**

Run: `pytest tests/test_extractor.py -k "invalid or valid_dates or invalid_etd" -v`
Expected: FAIL — `assert '30/02/2026' == '2026-02-30'`

- [ ] **Step 3: Cài đặt** — thay `_fmt_date` và các nhánh trong `parse_date_str`:
```python
from datetime import datetime


def _fmt_date(day: str, month: str, year: str, time_part: Optional[str]) -> Optional[str]:
    """DD/MM/YYYY[ HH:MM], or None when the date/time does not exist (e.g. 30/02, 25:00)."""
    try:
        datetime(int(year), int(month), int(day))
        hh, mm = (int(x) for x in time_part.split(":")) if time_part else (0, 0)
        if not (0 <= hh <= 23 and 0 <= mm <= 59):
            return None
    except ValueError:
        return None
    formatted = f"{int(day):02d}/{int(month):02d}/{year}"
    if time_part:
        formatted += f" {hh:02d}:{mm:02d}"
    return formatted
```
Trong `parse_date_str`, mỗi nhánh đổi `return _fmt_date(...)` thành:
```python
            formatted = _fmt_date(day, mon_num, year_full, time_part)
            return formatted if formatted else date_str
```
(tương tự cho nhánh `_RE_ISO` và `_RE_DMY`.) Cập nhật docstring: thêm dòng `- '2026-02-30' -> '2026-02-30' (invalid dates are returned unchanged)`.

- [ ] **Step 4: Chạy test**

Run: `pytest tests/test_extractor.py tests/test_image_extractor.py tests/test_booking_samples.py -v`
Expected: PASS toàn bộ

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/extractor.py tests/test_extractor.py
git commit -m "fix(extractor): keep impossible dates unchanged instead of reformatting them

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Chế độ giờ cố định chỉ chạy bù khi thật sự lỡ mốc (D3b)

**Files:**
- Modify: `backend/app/services/background_tasks.py:85-110,263-282,364-370`
- Test: `tests/test_scheduler.py` (fixture L22-38 + test mới)

**Interfaces:**
- Produces: `missed_times_slot(times: list[str], last_run_at: str | None, now: datetime) -> bool`; setting `auto_sync_last_run_at` (chuỗi giờ VN hoặc `""`); `load_settings()` nạp `last_run_at` từ DB.

- [ ] **Step 1: Viết test thất bại** — trong fixture `clean_scheduler_state` thêm `("auto_sync_last_run_at", "")` vào danh sách setting. Thêm test:
```python
def test_missed_times_slot():
    now = datetime(2026, 9, 15, 9, 0, tzinfo=VN)
    times = ["08:00", "14:00"]
    assert bt.missed_times_slot(times, None, now) is True
    assert bt.missed_times_slot(times, "2026-09-15 08:05:00", now) is False   # already ran after 08:00
    assert bt.missed_times_slot(times, "2026-09-15 07:59:00", now) is True    # 08:00 was missed
    assert bt.missed_times_slot(times, "2026-09-14 14:30:00", datetime(2026, 9, 15, 7, 0, tzinfo=VN)) is False
    assert bt.missed_times_slot(times, "2026-09-14 13:00:00", datetime(2026, 9, 15, 7, 0, tzinfo=VN)) is True
    assert bt.missed_times_slot([], None, now) is False
    assert bt.missed_times_slot(times, "garbage", now) is True


@patch("backend.app.services.background_tasks.run_sync_all", new_callable=AsyncMock)
@pytest.mark.parametrize("last_run,expect_kick", [("2099-01-01 00:00:00", False), ("2000-01-01 00:00:00", True)])
def test_times_mode_startup_kicks_only_when_slot_missed(mock_sync, last_run, expect_kick):
    set_system_setting("auto_sync_enabled", "1")
    set_system_setting("auto_sync_mode", "times")
    set_system_setting("auto_sync_times", '["08:00"]')
    set_system_setting("auto_sync_last_run_at", last_run)

    async def scenario():
        await bt.restore_auto_sync()
        return bt.scheduler.get_job(bt.AUTO_SYNC_KICK_JOB_ID)

    kick = asyncio.run(scenario())
    assert (kick is not None) is expect_kick
    assert bt.get_auto_sync_status()["last_run_at"] == last_run


def test_run_sync_all_persists_last_run_at():
    async def empty():
        return bt._empty_result()

    with patch.object(bt, "sync_vessel_watchlists", empty), patch.object(bt, "sync_container_watchlists", empty):
        asyncio.run(bt.run_sync_all())
    assert get_system_setting("auto_sync_last_run_at", "") == bt._state["last_run_at"]
    assert bt._state["last_run_at"]
```

- [ ] **Step 2: Chạy, xác nhận thất bại**

Run: `pytest tests/test_scheduler.py -v`
Expected: FAIL — `AttributeError: ... 'missed_times_slot'`

- [ ] **Step 3: Cài đặt**

Thêm sau `normalize_times`:
```python
def missed_times_slot(times: list[str], last_run_at: str | None, now: datetime) -> bool:
    """True when the most recent fixed HH:MM slot (VN time, at or before `now`) happened after the last run."""
    try:
        slots = normalize_times(times)
    except ValueError:
        return False
    if not slots:
        return False
    now = now.astimezone(VN_TZ)
    candidates = []
    for day_offset in (0, -1):
        day = (now + timedelta(days=day_offset)).date()
        for t in slots:
            h, m = (int(x) for x in t.split(":"))
            slot = datetime(day.year, day.month, day.day, h, m, tzinfo=VN_TZ)
            if slot <= now:
                candidates.append(slot)
    if not candidates:
        return False
    latest_slot = max(candidates)
    try:
        last = datetime.strptime(last_run_at or "", DATETIME_FMT).replace(tzinfo=VN_TZ)
    except ValueError:
        return True
    return last < latest_slot
```

`load_settings()` — trước `_state.update(...)`:
```python
    last_run_at = get_system_setting("auto_sync_last_run_at", "") or None
```
và thêm `"last_run_at": last_run_at` vào dict `_state.update`.

`run_sync_all()` — trong `finally` sau khi gán `_state["last_run_at"]`:
```python
            try:
                await asyncio.to_thread(set_system_setting, "auto_sync_last_run_at", _state["last_run_at"])
            except Exception:
                logger.exception("Could not persist auto_sync_last_run_at")
```

`restore_auto_sync()`:
```python
async def restore_auto_sync(startup_delay_seconds: float = STARTUP_DELAY_SECONDS):
    """Called from app lifespan: restore persisted settings and register jobs.
    Interval mode catches up after a short delay; fixed-times mode only when a slot was actually missed."""
    try:
        await asyncio.to_thread(load_settings)
        kick = None
        if _state["enabled"]:
            if _state["mode"] != "times" or missed_times_slot(_state["times"], _state["last_run_at"], datetime.now(VN_TZ)):
                kick = startup_delay_seconds
            else:
                logger.info("Fixed-times auto-sync: no slot missed since last run, skipping catch-up")
        apply_schedule(kick_delay_seconds=kick)
    except Exception:
        logger.exception("Could not restore auto-sync schedule")
```

- [ ] **Step 4: Chạy test**

Run: `pytest tests/test_scheduler.py -v`
Expected: PASS toàn bộ (kể cả `test_restore_on_startup_registers_job_with_delayed_first_run` của chế độ chu kỳ)

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/background_tasks.py tests/test_scheduler.py
git commit -m "fix(sync): fixed-time mode only catches up on startup when a slot was missed

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Khôi phục backup "Gộp" / "Thay thế" — backend (D3a)

**Files:**
- Modify: `backend/app/core/database.py:700-779`
- Modify: `backend/app/api/export_backup.py:42-48`
- Test: `backend/tests/test_backup_restore.py`

**Interfaces:**
- Produces: `RESTORE_MODES = ("merge", "replace")`; `import_backup_data(backup_data: dict, mode: str = "merge") -> None` (ValueError nếu mode sai); `POST /restore?mode=merge|replace` → `{status, message, mode}` (400 nếu mode sai).

- [ ] **Step 1: Viết test thất bại** — thêm vào `backend/tests/test_backup_restore.py`:
```python
import pytest
from backend.app.core import database as db


def _counts(col_name):
    col = next(c for c in db.get_collections() if c["name"] == col_name)
    return len(db.get_bookings(col["id"])), len(db.get_vessel_schedules(col["id"])), len(db.get_containers(col["id"]))


def _backup_with_one_collection(name="KHO A", booking_vessel="KOTA NEW"):
    return {
        "collections": [{
            "name": name, "created_at": "2026-01-01 00:00:00", "settings": None,
            "bookings": [{"booking_no": "SGN1", "pdf_name": "a.pdf", "vessel": booking_vessel}],
            "vessel_schedules": [{"site_id": "CTL", "vessel_name": "EVER", "in_out_voyage": "001N", "remarks": "from backup"}],
            "containers": [{"site_id": "CTL", "containerno": "ABCU1234567", "event_time": "t1", "event_type": "LOAD"}],
            "vessel_watchlists": [], "container_watchlists": [],
        }],
        "color_rules": [{"target_table": "booking", "column_key": "Carrier", "match_value": "PIL-TEST",
                         "match_type": "exact", "preset_id": "red", "is_enabled": 1}],
    }


def test_merge_into_same_name_collection_backup_wins(fresh_db):
    col = db.create_collection("KHO A")
    db.insert_booking(col, {"booking_no": "SGN1", "pdf_name": "a.pdf", "vessel": "KOTA OLD"})
    db.insert_booking(col, {"booking_no": "SGN2", "pdf_name": "b.pdf"})
    db.insert_vessel_schedules(col, [{"SITE_ID": "CTL", "VESSELNAME": "EVER", "IN_OUT_VOYAGE": "001N", "REMARKS": "local"}])
    db.create_color_rule({"target_table": "booking", "column_key": "Carrier", "match_value": "PIL-TEST",
                          "match_type": "exact", "preset_id": "blue"})

    db.import_backup_data(_backup_with_one_collection(), mode="merge")

    assert [c["name"] for c in db.get_collections()].count("KHO A") == 1
    bookings = db.get_bookings(col)
    assert sorted(b["Booking No"] for b in bookings) == ["SGN1", "SGN2"]
    assert next(b for b in bookings if b["Booking No"] == "SGN1")["Vessel"] == "KOTA NEW"
    assert db.get_vessel_schedules(col)[0]["remarks"] == "from backup"
    pil = [r for r in db.get_color_rules() if r["match_value"] == "PIL-TEST"]
    assert len(pil) == 1 and pil[0]["preset_id"] == "red"
    assert any(r["match_value"] == "CTL" for r in db.get_color_rules())  # untouched default rules survive merge


def test_merge_twice_is_idempotent(fresh_db):
    backup = _backup_with_one_collection()
    db.import_backup_data(backup, mode="merge")
    db.import_backup_data(backup, mode="merge")
    assert _counts("KHO A") == (1, 1, 1)


def test_replace_wipes_existing_data(fresh_db):
    other = db.create_collection("KHO CU")
    db.insert_booking(other, {"booking_no": "OLD"})
    db.import_backup_data(_backup_with_one_collection(), mode="replace")
    assert [c["name"] for c in db.get_collections()] == ["KHO A"]
    assert _counts("KHO A") == (1, 1, 1)
    assert [r["match_value"] for r in db.get_color_rules()] == ["PIL-TEST"]


def test_invalid_mode_rejected(fresh_db):
    with pytest.raises(ValueError):
        db.import_backup_data(_backup_with_one_collection(), mode="nuke")


def test_restore_endpoint_mode(fresh_db):
    from fastapi.testclient import TestClient
    from backend.app.main import app
    client = TestClient(app)
    assert client.post("/api/v1/restore?mode=nuke", json=_backup_with_one_collection()).status_code == 400
    res = client.post("/api/v1/restore?mode=replace", json=_backup_with_one_collection())
    assert res.status_code == 200 and res.json()["mode"] == "replace"
    assert client.post("/api/v1/restore", json=_backup_with_one_collection()).json()["mode"] == "merge"
```
Kiểm tra `DEFAULT_COLOR_RULES` có rule `match_value == "CTL"` (plan dựa trên seed cổng CTL); nếu tên khác, dùng một `match_value` mặc định có thật.

- [ ] **Step 2: Chạy, xác nhận thất bại**

Run: `pytest backend/tests/test_backup_restore.py -v`
Expected: FAIL — `TypeError: import_backup_data() got an unexpected keyword argument 'mode'`

- [ ] **Step 3: Cài đặt DB** — thêm helper sau `_insert_collection_unique` và thay `import_backup_data`:
```python
RESTORE_MODES = ("merge", "replace")


def _merge_collection(cursor: sqlite3.Cursor, name: str, created_at: str, settings) -> int:
    base_name = (name or "").strip() or "Imported Collection"
    row = cursor.execute("SELECT id FROM collections WHERE name = ?;", (base_name,)).fetchone()
    if not row:
        return _insert_collection_unique(cursor, base_name, created_at, settings)
    if settings is not None:
        cursor.execute("UPDATE collections SET settings = ? WHERE id = ?;", (settings, row[0]))
    return row[0]


def import_backup_data(backup_data: dict, mode: str = "merge"):
    """
    mode="merge": same-name collections are merged; on conflicts the backup row wins.
    mode="replace": all collections (with their rows) and color rules are deleted first.
    System settings are never touched. Runs in ONE transaction.
    """
    if mode not in RESTORE_MODES:
        raise ValueError(f"Invalid restore mode: {mode}")
    if not isinstance(backup_data, dict) or "collections" not in backup_data:
        raise ValueError("Invalid backup format")
    collections = backup_data.get("collections") or []
    if not isinstance(collections, list):
        raise ValueError("Invalid backup format")
    color_rules = backup_data.get("color_rules")
    has_rules = isinstance(color_rules, list) and bool(color_rules)

    now_str = _now_str()
    with get_connection() as conn:
        cursor = conn.cursor()
        if mode == "replace":
            cursor.execute("DELETE FROM collections;")  # ON DELETE CASCADE removes rows + watchlists
            if has_rules:
                cursor.execute("DELETE FROM color_rules;")

        for col_data in collections:
            if not isinstance(col_data, dict):
                continue
            created_at = col_data.get("created_at") or now_str
            col_id = _merge_collection(cursor, col_data.get("name"), created_at, col_data.get("settings"))

            for b in col_data.get("bookings") or []:
                booking_no = (b.get("Booking No", b.get("booking_no")) or "").strip()
                pdf_name = b.get("Tên file PDF", b.get("pdf_name")) or ""
                if booking_no:
                    cursor.execute(
                        "DELETE FROM bookings WHERE collection_id = ? AND booking_no = ? AND COALESCE(pdf_name, '') = ?;",
                        (col_id, booking_no, pdf_name))
                _insert_booking_row(cursor, col_id, b)

            vessels = col_data.get("vessel_schedules") or []
            if vessels:
                _insert_vessel_rows(cursor, col_id, vessels, now_str, preserve_queried_at=True)

            containers = col_data.get("containers") or []
            if containers:
                _insert_container_rows(cursor, col_id, containers, now_str, preserve_queried_at=True)

            # (giữ nguyên 2 khối executemany vessel_watchlists / container_watchlists hiện có)

        if has_rules:
            latest = {}
            for cr in color_rules:
                if isinstance(cr, dict) and cr.get("match_value") not in (None, ""):
                    key = _color_rule_key(cr)
                    latest.pop(key, None)
                    latest[key] = cr
            if mode == "merge":
                cursor.executemany(
                    "DELETE FROM color_rules WHERE target_table = ? AND column_key = ? AND match_value = ? AND match_type = ?;",
                    list(latest.keys()))
            cursor.executemany(_COLOR_RULE_INSERT_SQL, [
                _color_rule_params(cr, cr.get("created_at") or now_str) for cr in latest.values()
            ])
```
Lưu ý: backup cũ lưu booking theo key hiển thị (`"Booking No"`, `"Tên file PDF"` — xem `export_backup_data` dùng `get_bookings`) nên đọc cả hai dạng key như trên.

- [ ] **Step 4: Cài đặt API** — `export_backup.py`:
```python
from fastapi import APIRouter, HTTPException, Query
```
```python
@router.post("/restore")
def restore_backup(payload: dict, mode: str = Query("merge", description="merge | replace")):
    if mode not in ("merge", "replace"):
        raise HTTPException(status_code=400, detail="mode must be 'merge' or 'replace'")
    try:
        import_backup_data(payload, mode=mode)
        return {"status": "success", "message": "Database restored successfully", "mode": mode}
    except Exception as e:
        raise HTTPException(status_code=400, detail=f"Restore failed: {e}")
```

- [ ] **Step 5: Chạy test**

Run: `pytest backend/tests/test_backup_restore.py tests/test_eport.py -v`
Expected: PASS toàn bộ (kể cả `test_container_backup_restore` cũ)

- [ ] **Step 6: Commit**

```bash
git add backend/app/core/database.py backend/app/api/export_backup.py backend/tests/test_backup_restore.py
git commit -m "feat(backup): restore in merge (default) or replace mode

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: Khôi phục backup "Gộp" / "Thay thế" — giao diện (D3a)

**Files:**
- Modify: `frontend/src/services/api.ts:299-307`
- Modify: `frontend/src/components/common/BackupModal.tsx`
- Modify: `frontend/src/i18n/translations.ts` (section `common`, vi ~L97, en ~L763)

**Interfaces:**
- Consumes: `POST /restore?mode=` (Task 3); `useConfirm()` (`hooks/useConfirm.tsx`)
- Produces: `export type RestoreMode = 'merge' | 'replace'`; `restoreBackupDb(backupData: unknown, mode?: RestoreMode): Promise<{status: string; message: string; mode: RestoreMode}>`

- [ ] **Step 1: API + chuỗi**

`api.ts`:
```ts
export type RestoreMode = 'merge' | 'replace';

export const restoreBackupDb = async (backupData: unknown, mode: RestoreMode = 'merge') => {
  const res = await apiClient.post<{ status: string; message: string; mode: RestoreMode }>(
    '/restore', backupData, { params: { mode }, timeout: LONG_TIMEOUT });
  return res.data;
};
```

`translations.ts` — trong `vi.common` thêm:
```ts
      restoreDesc: "Nhập file JSON sao lưu để phục hồi dữ liệu vào hệ thống.",
      restoreMerge: "Gộp",
      restoreMergeDesc: "Giữ dữ liệu hiện có; bộ sưu tập trùng tên được gộp, dòng trùng lấy theo bản sao lưu.",
      restoreReplace: "Thay thế",
      restoreReplaceDesc: "Xoá toàn bộ bộ sưu tập và quy tắc màu hiện có, chỉ giữ dữ liệu trong bản sao lưu.",
      restoreReplaceConfirmTitle: "Thay thế toàn bộ dữ liệu?",
      restoreReplaceConfirmMessage: "Mọi bộ sưu tập, booking, lịch tàu, container, danh sách theo dõi và quy tắc màu hiện có sẽ bị xoá và thay bằng nội dung file sao lưu. Không thể hoàn tác.",
      restoreReplaceConfirm: "Xoá và thay thế",
```
trong `en.common`:
```ts
      restoreDesc: "Import a JSON backup file to restore data.",
      restoreMerge: "Merge",
      restoreMergeDesc: "Keep existing data; same-name collections are merged and the backup wins on duplicates.",
      restoreReplace: "Replace",
      restoreReplaceDesc: "Delete all current collections and color rules, keep only what is in the backup.",
      restoreReplaceConfirmTitle: "Replace all data?",
      restoreReplaceConfirmMessage: "All collections, bookings, vessel schedules, containers, watchlists and color rules will be deleted and replaced by the backup. This cannot be undone.",
      restoreReplaceConfirm: "Delete and replace",
```

- [ ] **Step 2: BackupModal**

Thêm state + confirm, thay `handleFileChange`:
```tsx
import { useConfirm } from '../../hooks/useConfirm';
import { getBackupDb, restoreBackupDb, RestoreMode } from '../../services/api';
```
```tsx
  const confirm = useConfirm();
  const [restoreMode, setRestoreMode] = useState<RestoreMode>('merge');

  const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const input = e.target;
    const file = input.files?.[0];
    if (!file) return;
    try {
      const jsonData = JSON.parse(await file.text());
      if (restoreMode === 'replace') {
        const ok = await confirm({
          title: t.common.restoreReplaceConfirmTitle,
          message: t.common.restoreReplaceConfirmMessage,
          confirmText: t.common.restoreReplaceConfirm,
          danger: true,
        });
        if (!ok) return;
      }
      setLoading(true);
      await restoreBackupDb(jsonData, restoreMode);
      addToast(t.common.success, 'success');
      await refreshCollections();
      onClose();
    } catch (err: any) {
      addToast(err?.response?.data?.detail || err.message || 'Invalid JSON backup file', 'error');
    } finally {
      setLoading(false);
      input.value = '';
    }
  };
```
Thay đoạn mô tả cứng tiếng Việt bằng `{t.common.restoreDesc}` và chèn trước `<label>` chọn file:
```tsx
          <div role="radiogroup" className="grid grid-cols-2 gap-2 mt-3">
            {(['merge', 'replace'] as RestoreMode[]).map((mode) => {
              const active = restoreMode === mode;
              const danger = mode === 'replace';
              return (
                <button
                  key={mode}
                  type="button"
                  role="radio"
                  aria-checked={active}
                  disabled={loading}
                  onClick={() => setRestoreMode(mode)}
                  className={`text-left p-2.5 rounded-xl border text-xs transition-all ${
                    active
                      ? danger
                        ? 'border-rose-400 bg-rose-50 dark:bg-rose-950/40 dark:border-rose-700'
                        : 'border-primary-400 bg-primary-50 dark:bg-primary-950/40 dark:border-primary-700'
                      : 'border-slate-200 dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700/50'
                  }`}
                >
                  <div className={`font-semibold ${danger && active ? 'text-rose-700 dark:text-rose-300' : 'text-slate-900 dark:text-slate-100'}`}>
                    {mode === 'merge' ? t.common.restoreMerge : t.common.restoreReplace}
                  </div>
                  <div className="text-[11px] text-slate-500 dark:text-slate-400 mt-0.5">
                    {mode === 'merge' ? t.common.restoreMergeDesc : t.common.restoreReplaceDesc}
                  </div>
                </button>
              );
            })}
          </div>
```

- [ ] **Step 3: Build + thủ công**

Run: `cd frontend && npm run build`
Thủ công (`npm run dev`): Sao lưu → khôi phục "Gộp" 3 lần → số bộ sưu tập, booking, color rules không tăng. Chọn "Thay thế" → hộp xác nhận đỏ; Huỷ → không đổi gì; Đồng ý → chỉ còn dữ liệu trong file, tab hiện bộ sưu tập đầu tiên.
Expected: đúng.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/services/api.ts frontend/src/components/common/BackupModal.tsx frontend/src/i18n/translations.ts
git commit -m "feat(backup-ui): choose merge or replace when restoring a backup

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: Tra tàu nhanh không tự lưu vào tab Tàu (D3c)

**Files:**
- Modify: `backend/app/schemas/models.py:32-36`
- Modify: `backend/app/api/vessels.py:34-52`
- Test: `backend/tests/test_bulk_api.py`
- Modify: `frontend/src/services/api.ts:126-134`, `frontend/src/components/booking/QuickVesselSearch.tsx:134-162,300-324`, `frontend/src/i18n/translations.ts` (`booking.quickVessel`, vi L332-359, en L998-1025)

**Interfaces:**
- Produces (HTTP): `POST /vessels/search` body thêm `save?: boolean` (mặc định `true`), response thêm `saved: boolean`; `POST /vessels/save` body `{collection_id, items: object[]}` → `{status: "success", saved: number}`
- Produces (TS): `searchVesselsApi(collectionId, siteId, vesselName, voyage?, options?: { save?: boolean })`; `saveVesselResultsApi(collectionId: number, items: Record<string, any>[]): Promise<{status: string; saved: number}>`

- [ ] **Step 1: Viết test thất bại** — thêm vào `backend/tests/test_bulk_api.py`:
```python
def test_vessel_search_save_flag_and_save_endpoint(client, monkeypatch):
    col = db.create_collection("QV")
    item = {"SITE_ID": "CTL", "VESSELNAME": "EVER MEMO", "IN_OUT_VOYAGE": "012E"}
    monkeypatch.setattr(vessels_api, "search_vessels_detailed", lambda *a, **k: {
        "items": [item], "reason": "ok", "message": "", "available_voyages": []})

    res = client.post(f"{API}/vessels/search", json={"collection_id": col, "site_id": "CTL",
                                                     "vessel_name": "EVER MEMO", "voyage": "012E", "save": False})
    assert res.status_code == 200 and res.json()["count"] == 1 and res.json()["saved"] is False
    assert db.get_vessel_schedules(col) == []

    res = client.post(f"{API}/vessels/save", json={"collection_id": col, "items": [item]})
    assert res.json() == {"status": "success", "saved": 1}
    assert len(db.get_vessel_schedules(col)) == 1

    res = client.post(f"{API}/vessels/search", json={"collection_id": col, "site_id": "CTL", "vessel_name": "EVER MEMO"})
    assert res.json()["saved"] is True  # default stays backward compatible
```

- [ ] **Step 2: Chạy, xác nhận thất bại**

Run: `pytest backend/tests/test_bulk_api.py::test_vessel_search_save_flag_and_save_endpoint -v`
Expected: FAIL — `AttributeError: ... has no attribute 'search_vessels_detailed'`

- [ ] **Step 3: Cài đặt backend**

`models.py`:
```python
class VesselSearchRequest(BaseModel):
    collection_id: int
    site_id: str
    vessel_name: str
    voyage: Optional[str] = None
    save: Optional[bool] = True

class VesselSaveResultsRequest(BaseModel):
    collection_id: int
    items: List[dict]
```

`vessels.py` — import `search_vessels_detailed` (bên cạnh `search_vessels`) và `VesselSaveResultsRequest`; thay `query_vessels`:
```python
@router.post("/search")
def query_vessels(payload: VesselSearchRequest):
    logger.info(f"[API /vessels/search] site='{payload.site_id}', vessel='{payload.vessel_name}', voyage='{payload.voyage}', collection_id={payload.collection_id}, save={payload.save}")
    try:
        detail = search_vessels_detailed(payload.site_id, payload.vessel_name, payload.voyage)
        results = detail.get("items") or []
        saved = bool(results) and payload.save is not False
        if saved:
            insert_vessel_schedules(payload.collection_id, results)
        if results:
            message = detail.get("message") or ""
        elif payload.voyage:
            message = f"Không tìm thấy lịch tàu '{payload.vessel_name}' khớp với số chuyến '{payload.voyage}' trên ePort"
        else:
            message = f"Không tìm thấy thông tin lịch tàu '{payload.vessel_name}' trên ePort"
        return {"count": len(results), "items": results, "message": message, "saved": saved,
                "match": detail.get("reason") if results else None}
    except Exception as e:
        logger.exception(f"[API /vessels/search] ❌ Error querying vessels: {e}")
        raise HTTPException(status_code=400, detail=str(e))

@router.post("/save")
def save_vessel_results(payload: VesselSaveResultsRequest):
    items = [it for it in payload.items if isinstance(it, dict) and (it.get("VESSELNAME") or it.get("vessel_name"))]
    if items:
        insert_vessel_schedules(payload.collection_id, items)
    return {"status": "success", "saved": len(items)}
```
(`match` = `"ok"` hoặc `"fuzzy"` sau Task 7.)

- [ ] **Step 4: Chạy test backend**

Run: `pytest backend/tests/test_bulk_api.py tests/test_api.py -v`
Expected: PASS

- [ ] **Step 5: Frontend**

`api.ts`:
```ts
export const searchVesselsApi = async (
  collectionId: number, siteId: string, vesselName: string, voyage?: string, options: { save?: boolean } = {}
): Promise<any> => {
  const res = await apiClient.post('/vessels/search', {
    collection_id: collectionId, site_id: siteId, vessel_name: vesselName, voyage: voyage || null,
    save: options.save ?? true,
  });
  return res.data;
};

export const saveVesselResultsApi = async (collectionId: number, items: Record<string, any>[]) =>
  (await apiClient.post<{ status: string; saved: number }>('/vessels/save', { collection_id: collectionId, items })).data;
```

`QuickVesselSearch.tsx`:
- Import thêm `saveVesselResultsApi` và icon `Save` từ `lucide-react`.
- State: `const [savedKeys, setSavedKeys] = useState<Set<string>>(new Set()); const [savingKey, setSavingKey] = useState<string | null>(null);` — reset `setSavedKeys(new Set())` ngay khi bắt đầu một lần tra mới.
- Dòng 136: `const res = await searchVesselsApi(activeCollection.id, site, vesselName, voyage.trim(), { save: false });`
- Thêm hàm:
```tsx
  const saveRow = async (row: Record<string, any>, key: string, silent = false) => {
    if (!activeCollection || savedKeys.has(key)) return;
    setSavingKey(key);
    try {
      await saveVesselResultsApi(activeCollection.id, [row]);
      setSavedKeys((prev) => new Set(prev).add(key));
      if (!silent) addToast(tf(qv.saveSuccess, { count: 1 }), 'success');
    } catch (err: any) {
      addToast(errorText(err, t.common.error), 'error');
    } finally {
      setSavingKey(null);
    }
  };
```
- `handleWatch` thêm tham số tuỳ chọn `row?: Record<string, any>`; sau khi `addVesselWatchlist` thành công: `if (row) await saveRow(row, key, true);`. Nút "Theo dõi" trong danh sách kết quả gọi `handleWatch(rSite, rName, rVoy, row)`; nút ở trường hợp không tìm thấy giữ nguyên (không có row).
- Trước nút "Theo dõi" trong mỗi kết quả (L316) chèn:
```tsx
                        <button
                          type="button"
                          disabled={savedKeys.has(key) || savingKey === key}
                          onClick={() => saveRow(row, key)}
                          className="flex items-center gap-1 px-2 py-1 rounded-lg text-[11px] font-bold border border-emerald-300 dark:border-emerald-800 text-emerald-700 dark:text-emerald-300 hover:bg-emerald-50 dark:hover:bg-emerald-950/50 disabled:opacity-60"
                        >
                          {savingKey === key ? <Loader2 className="w-3 h-3 animate-spin" /> : savedKeys.has(key) ? <Check className="w-3 h-3" /> : <Save className="w-3 h-3" />}
                          {savedKeys.has(key) ? qv.saved : qv.save}
                        </button>
```

`translations.ts` — `vi.booking.quickVessel`: đổi `found: "Tìm thấy {count} lịch tàu"`, `savedHint: "Kết quả chưa được lưu. Bấm \"Lưu vào tab Tàu\" hoặc \"Theo dõi\" để lưu."`; thêm `save: "Lưu vào tab Tàu"`, `saved: "Đã lưu"`, `saveSuccess: "Đã lưu {count} lịch tàu vào tab Tàu"`. `en.booking.quickVessel`: `found: "Found {count} schedule(s)"`, `savedHint: "Results are not saved yet. Click \"Save to Vessels tab\" or \"Watch\" to keep them."`, `save: "Save to Vessels tab"`, `saved: "Saved"`, `saveSuccess: "Saved {count} schedule(s) to the Vessels tab"`.
`savedHint` hiện chỉ render khi `!onNavigateTab` (L354-356) — đổi thành luôn render khi `results && results.length > 0`.

- [ ] **Step 6: Build + thủ công**

Run: `cd frontend && npm run build`
Thủ công: tra "EVER" từ một booking → tab Tàu không có dòng mới; bấm "Lưu vào tab Tàu" → dòng xuất hiện; tra lại tàu khác, bấm "Theo dõi" → có trong watchlist **và** tab Tàu. Hành động hàng loạt "Tra tàu" ở tab Booking vẫn lưu.
Expected: đúng.

- [ ] **Step 7: Commit**

```bash
git add backend/app/schemas/models.py backend/app/api/vessels.py backend/tests/test_bulk_api.py frontend/src/services/api.ts frontend/src/components/booking/QuickVesselSearch.tsx frontend/src/i18n/translations.ts
git commit -m "fix(quick-vessel): do not auto-save lookups; save only on Save or Watch

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: Tra lại container chỉ lưu đúng loại sự kiện (D3d)

**Files:**
- Modify: `backend/app/schemas/models.py:91-92`, `backend/app/api/containers.py:62-109`
- Test: `backend/tests/test_bulk_api.py`
- Modify: `frontend/src/services/api.ts:269-272`, `frontend/src/components/container/ContainerTab.tsx:573-615`, `frontend/src/i18n/translations.ts` (`bulk`, vi ~L187, en ~L853)

**Interfaces:**
- Produces (HTTP): `POST /containers/resync` body `{ids: number[], all_events?: boolean}` (mặc định `false`); response không đổi.
- Produces (TS): `resyncContainersApi(ids: number[], options?: { allEvents?: boolean }): Promise<ResyncResult>`
- Quy tắc lọc: kết quả ePort được lưu khi `all_events` **hoặc** `EVENT_TYPE` (upper/trim) nằm trong tập `event_type` của các dòng đã chọn cho container đó; dòng đã chọn có `event_type` rỗng → nhận mọi sự kiện của container đó. `not_found` = container không có kết quả **nào được lưu**.

- [ ] **Step 1: Viết test thất bại** — thêm vào `backend/tests/test_bulk_api.py`:
```python
def test_container_resync_filters_by_selected_event_type(client, monkeypatch):
    col = db.create_collection("RS")
    ids = db.insert_containers(col, [{"SITE": "CTL", "CONTAINERNO": "ABCU1234567", "EVENT_TIME": "t0", "EVENT_TYPE": "LOAD"}])
    eport_rows = [
        {"CONTAINERNO": "ABCU1234567", "EVENT_TIME": "t1", "EVENT_TYPE": "LOAD"},
        {"CONTAINERNO": "ABCU1234567", "EVENT_TIME": "t2", "EVENT_TYPE": "UNLOAD"},
    ]
    monkeypatch.setattr(containers_api, "search_containers", lambda *a, **k: eport_rows)

    res = client.post(f"{API}/containers/resync", json={"ids": ids})
    assert res.json()["updated"] == 1
    assert sorted(c["event_type"] for c in db.get_containers(col)) == ["LOAD", "LOAD"]

    res = client.post(f"{API}/containers/resync", json={"ids": ids, "all_events": True})
    assert res.json()["updated"] == 2
    assert "UNLOAD" in {c["event_type"] for c in db.get_containers(col)}


def test_container_resync_reports_not_found_when_event_missing(client, monkeypatch):
    col = db.create_collection("RS2")
    ids = db.insert_containers(col, [{"SITE": "CTL", "CONTAINERNO": "ABCU7654321", "EVENT_TIME": "t0", "EVENT_TYPE": "OUTGATE"}])
    monkeypatch.setattr(containers_api, "search_containers",
                        lambda *a, **k: [{"CONTAINERNO": "ABCU7654321", "EVENT_TIME": "t1", "EVENT_TYPE": "LOAD"}])
    body = client.post(f"{API}/containers/resync", json={"ids": ids}).json()
    assert body["updated"] == 0 and body["not_found"] == ["ABCU7654321"]
```

- [ ] **Step 2: Chạy, xác nhận thất bại**

Run: `pytest backend/tests/test_bulk_api.py -k container_resync -v`
Expected: FAIL — `assert 2 == 1`

- [ ] **Step 3: Cài đặt backend**

`models.py`:
```python
class ContainerResyncRequest(BaseModel):
    ids: List[int]
    all_events: Optional[bool] = False
```

`containers.py` — import `ContainerResyncRequest`; thay `resync_containers`:
```python
@router.post("/resync", response_model=ResyncResponse)
def resync_containers(payload: ContainerResyncRequest):
    """Re-query ePort for the selected rows (one call per site). Only events of the selected rows' types are
    saved unless all_events=true."""
    rows = get_containers_by_ids(payload.ids)

    # site_id -> container_no -> {"collections": set[int], "events": set[str] ("" = any event)}
    by_site: dict[str, dict[str, dict]] = {}
    for r in rows:
        cont_no = (r.get("containerno") or "").strip().upper()
        if not cont_no:
            continue
        site_id = (r.get("site_id") or "").strip().upper()
        entry = by_site.setdefault(site_id, {}).setdefault(cont_no, {"collections": set(), "events": set()})
        entry["collections"].add(r["collection_id"])
        entry["events"].add((r.get("event_type") or "").strip().upper())

    updated = 0
    not_found: list[str] = []
    errors: list[str] = []
    for site_id, conts in by_site.items():
        cont_nos = sorted(conts)
        try:
            results = search_containers(site_id, ",".join(cont_nos)) or []
        except Exception as e:
            logger.exception(f"[API /containers/resync] ❌ Error resyncing site '{site_id}': {e}")
            errors.append(f"{site_id or '-'}: {e}")
            continue

        found: set[str] = set()
        per_collection: dict[int, list[dict]] = {}
        for res in results:
            cont_no = _result_container_no(res)
            entry = conts.get(cont_no)
            if not entry:
                continue
            event = str(res.get("EVENT_TYPE", res.get("event_type", "")) or "").strip().upper()
            if not (payload.all_events or "" in entry["events"] or event in entry["events"]):
                continue
            found.add(cont_no)
            for col_id in entry["collections"]:
                per_collection.setdefault(col_id, []).append(res)

        try:
            for col_id, items in per_collection.items():
                insert_containers(col_id, items)
                updated += len(items)
        except Exception as e:
            logger.exception(f"[API /containers/resync] ❌ Error saving results for site '{site_id}': {e}")
            errors.append(f"{site_id or '-'}: {e}")

        not_found.extend(c for c in cont_nos if c not in found)

    logger.info(f"[API /containers/resync] Done. all_events={payload.all_events}, updated={updated}, not_found={len(not_found)}, errors={len(errors)}")
    return {"status": "success", "updated": updated, "not_found": not_found, "errors": errors}
```

- [ ] **Step 4: Chạy test backend**

Run: `pytest backend/tests/test_bulk_api.py -v`
Expected: PASS toàn bộ

- [ ] **Step 5: Frontend**

`api.ts`:
```ts
export const resyncContainersApi = async (ids: number[], options: { allEvents?: boolean } = {}): Promise<ResyncResult> => {
  const res = await apiClient.post<ResyncResult>('/containers/resync', { ids, all_events: !!options.allEvents }, { timeout: LONG_TIMEOUT });
  return res.data;
};
```

`ContainerTab.tsx` — đổi `handleBatchResync` nhận tham số và thêm action:
```tsx
  const handleBatchResync = (allEvents = false) =>
    runBulk(allEvents ? 'resync-all' : 'resync', async () => {
      const ids = selectedIdList;
      if (ids.length === 0) return;
      const res = await resyncContainersApi(ids, { allEvents });
      // ...giữ nguyên phần toast và loadData('refresh')...
    });
```
Trong `bulkActions`:
```tsx
    { key: 'resync', label: t.bulk.resync, title: t.bulk.resyncSameEventTooltip, icon: RotateCw, onClick: () => handleBatchResync(false), loading: bulkBusy === 'resync', disabled: !!bulkBusy },
    { key: 'resync-all', label: t.bulk.resyncAllEvents, title: t.bulk.resyncAllEventsTooltip, icon: RotateCw, onClick: () => handleBatchResync(true), loading: bulkBusy === 'resync-all', disabled: !!bulkBusy },
```
Kiểm tra kiểu của tham số `runBulk` (key string) cho phép `'resync-all'`; nếu là union type thì thêm vào union.

`translations.ts` — `vi.bulk`: `resyncSameEventTooltip: "Chỉ cập nhật sự kiện cùng loại với các dòng đã chọn"`, `resyncAllEvents: "Tra lại (mọi sự kiện)"`, `resyncAllEventsTooltip: "Lưu tất cả sự kiện ePort trả về cho các container đã chọn"`. `en.bulk`: `resyncSameEventTooltip: "Only update events of the same type as the selected rows"`, `resyncAllEvents: "Re-query (all events)"`, `resyncAllEventsTooltip: "Save every event ePort returns for the selected containers"`.
(`t.bulk.resync` dùng chung ở tab Tàu — không đổi nhãn.)

- [ ] **Step 6: Build + thủ công**

Run: `cd frontend && npm run build`
Thủ công: lọc pill LOAD, chọn 1 container, "Tra lại ePort" → không xuất hiện dòng UNLOAD/INGATE mới; "Tra lại (mọi sự kiện)" → xuất hiện.
Expected: đúng.

- [ ] **Step 7: Commit**

```bash
git add backend/app/schemas/models.py backend/app/api/containers.py backend/tests/test_bulk_api.py frontend/src/services/api.ts frontend/src/components/container/ContainerTab.tsx frontend/src/i18n/translations.ts
git commit -m "fix(container-resync): only save events matching the selected rows' event types

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: Khớp voyage gần đúng — backend (D2)

**Files:**
- Modify: `backend/app/services/eport_client.py:35-42,193-212`
- Modify: `backend/app/core/database.py` (`init_db` vessel_schedules, `_insert_vessel_rows` L785-816)
- Modify: `backend/app/services/background_tasks.py:159-166`, `backend/app/api/vessels.py:140-178`
- Test: `tests/test_eport.py`

**Interfaces:**
- Produces:
  - `voyage_core(part: str) -> str`, `voyage_cores(voyage: str) -> set[str]`, `is_voyage_fuzzy_match(query_voy: str, eport_voy: str) -> bool`
  - `search_vessels_detailed(...)["reason"]` thêm giá trị `"fuzzy"`; mỗi item khớp gần đúng có `"VOYAGE_MATCH": "fuzzy"`, `"REQUESTED_VOYAGE": <voyage người dùng nhập>`; `message = "Đã khớp gần đúng: chuyến '<yêu cầu>' ≈ '<ePort>'"`
  - Cột `vessel_schedules.requested_voyage TEXT` (rỗng = khớp chính xác)
- Quy tắc chuẩn hoá (`voyage_core`): viết hoa; bỏ tiền tố `VOYAGE`/`VOY`/`V.`; bỏ ký tự không phải chữ/số; bỏ hậu tố hướng `[NSEW]+` đứng sau chữ số; bỏ số 0 đầu. `voyage_cores` tách theo `/ \ |`, và với phía ePort thêm lõi của từng phần tách theo `-` (độ dài ≥ 2). Chỉ nhận khi đúng **một** giá trị `IN_OUT_VOYAGE` khác nhau khớp.

- [ ] **Step 1: Viết test thất bại** — thêm vào `tests/test_eport.py`:
```python
import pytest
from backend.app.services.eport_client import voyage_core, voyage_cores, is_voyage_fuzzy_match


@pytest.mark.parametrize("raw,core", [
    ("0272S", "272"), ("272N", "272"), ("V.0272S", "272"), ("VOY 272", "272"),
    ("1461-012E", "1461012"), ("2615W", "2615"), ("ABC12", "ABC12"), ("", ""),
])
def test_voyage_core(raw, core):
    assert voyage_core(raw) == core


@pytest.mark.parametrize("query,eport,expected", [
    ("0272S", "272S/272N", True),
    ("272", "0272N", True),
    ("012E", "1461-012E", True),
    ("V.2615W", "2615W/2615E", True),
    ("273S", "272S/272N", False),
    ("", "272S", False),
])
def test_is_voyage_fuzzy_match(query, eport, expected):
    assert is_voyage_fuzzy_match(query, eport) is expected


@patch("backend.app.services.eport_client.requests.post")
def test_search_vessels_detailed_fuzzy_single_candidate(mock_post):
    from backend.app.services.eport_client import search_vessels_detailed
    mock_post.return_value = _vessel_response([
        {"VESSELNAME": "KOTA NEKAD", "IN_OUT_VOYAGE": "272S/272N"},
        {"VESSELNAME": "KOTA NEKAD", "IN_OUT_VOYAGE": "273S/273N"},
    ])
    detail = search_vessels_detailed("CTL", "KOTA NEKAD", "0272S")
    assert detail["reason"] == "fuzzy"
    assert [m["IN_OUT_VOYAGE"] for m in detail["items"]] == ["272S/272N"]
    assert detail["items"][0]["VOYAGE_MATCH"] == "fuzzy"
    assert detail["items"][0]["REQUESTED_VOYAGE"] == "0272S"
    assert "gần đúng" in detail["message"]


@patch("backend.app.services.eport_client.requests.post")
def test_search_vessels_detailed_fuzzy_ambiguous_is_mismatch(mock_post):
    from backend.app.services.eport_client import search_vessels_detailed
    mock_post.return_value = _vessel_response([
        {"VESSELNAME": "EVER MEMO", "IN_OUT_VOYAGE": "1461-012E"},
        {"VESSELNAME": "EVER MEMO", "IN_OUT_VOYAGE": "1462-012E"},
    ])
    detail = search_vessels_detailed("CTL", "EVER MEMO", "012E")
    assert detail["items"] == [] and detail["reason"] == "voyage_mismatch"


def test_requested_voyage_column_round_trip():
    from backend.app.core import database as db
    db.init_db()
    col = db.create_collection("FUZZY-COL")
    db.insert_vessel_schedules(col, [{"SITE_ID": "CTL", "VESSELNAME": "KOTA", "IN_OUT_VOYAGE": "272S/272N",
                                      "VOYAGE_MATCH": "fuzzy", "REQUESTED_VOYAGE": "0272S"}])
    assert db.get_vessel_schedules(col)[0]["requested_voyage"] == "0272S"
    db.insert_vessel_schedules(col, [{"SITE_ID": "CTL", "VESSELNAME": "KOTA", "IN_OUT_VOYAGE": "272S/272N"}])
    assert db.get_vessel_schedules(col)[0]["requested_voyage"] == ""
```
(`_vessel_response` và `patch` đã có sẵn trong file; test fixture `isolated_test_db` của `tests/conftest.py` cô lập DB.)

- [ ] **Step 2: Chạy, xác nhận thất bại**

Run: `pytest tests/test_eport.py -v`
Expected: FAIL — `ImportError: cannot import name 'voyage_core'`

- [ ] **Step 3: Cài đặt `eport_client.py`** — sau `is_voyage_match`:
```python
_VOY_PREFIX_RE = re.compile(r"^(?:VOYAGE|VOY)[.\s:]*|^V[.\s:]+")


def voyage_core(part: str) -> str:
    """'V.0272S' -> '272', '1461-012E' -> '1461012'. Used only for approximate matching."""
    s = _VOY_PREFIX_RE.sub("", (part or "").strip().upper())
    s = re.sub(r"[^A-Z0-9]", "", s)
    s = re.sub(r"(?<=\d)[NSEW]+$", "", s)
    return s.lstrip("0")


def voyage_cores(voyage: str, split_dashes: bool = False) -> set[str]:
    cores = set()
    for part in re.split(r"[/\\|]", voyage or ""):
        cores.add(voyage_core(part))
        if split_dashes and "-" in part:
            cores.update(voyage_core(p) for p in part.split("-") if len(p.strip()) >= 2)
    cores.discard("")
    return cores


def is_voyage_fuzzy_match(query_voy: str, eport_voy: str) -> bool:
    q = voyage_cores(query_voy)
    return bool(q) and bool(q & voyage_cores(eport_voy, split_dashes=True))
```
Trong `search_vessels_detailed`, thay khối sau `if matched: return _result(matched, "ok")` (L197-210):
```python
                if matched:
                    return _result(matched, "ok")
                available = []
                for m in cleaned_models:
                    v = m.get("IN_OUT_VOYAGE")
                    if v and v not in available:
                        available.append(v)
                fuzzy = [m for m in cleaned_models if is_voyage_fuzzy_match(voyage_query, m.get("IN_OUT_VOYAGE", ""))]
                fuzzy_voyages = sorted({m.get("IN_OUT_VOYAGE", "") for m in fuzzy})
                if len(fuzzy_voyages) == 1:
                    for m in fuzzy:
                        m["VOYAGE_MATCH"] = "fuzzy"
                        m["REQUESTED_VOYAGE"] = voyage_query
                    logger.info(f"[ePort VESSEL SEARCH] Fuzzy voyage match '{voyage_query}' ≈ '{fuzzy_voyages[0]}'")
                    return _result(fuzzy, "fuzzy",
                                   f"Đã khớp gần đúng: chuyến '{voyage_query}' ≈ '{fuzzy_voyages[0]}'", available)
                logger.warning(
                    f"[ePort VESSEL SEARCH] Chuyến tàu ePort KHÔNG trùng khớp với số chuyến yêu cầu '{voyage_query}'. "
                    f"Các chuyến hiện có trên ePort: {available}. -> Bỏ qua, KHÔNG cập nhật."
                )
                return _result([], "voyage_mismatch",
                               f"Không có chuyến '{voyage_query}' trên ePort. Các chuyến hiện có: {', '.join(available)}",
                               available)
```
Cập nhật docstring `reason`: thêm `"fuzzy"`.

- [ ] **Step 4: Cài đặt DB** — `init_db()` sau `CREATE TABLE IF NOT EXISTS vessel_schedules` (L330):
```python
        _ensure_columns(conn, "vessel_schedules", [("requested_voyage", "TEXT DEFAULT ''")])
```
Thêm cột `requested_voyage TEXT DEFAULT ''` vào câu `CREATE TABLE` (sau `queried_at TEXT,`). `_insert_vessel_rows`: thêm `requested_voyage` vào danh sách cột + một `?` và giá trị
```python
            s.get("REQUESTED_VOYAGE", s.get("requested_voyage", "")) or "",
```
đặt trước `row_queried_at`.

- [ ] **Step 5: Thông điệp watchlist**

`background_tasks.py` (L163-165) — thay message ok:
```python
                    ok_msg = f"Đã cập nhật {len(schedules)} lịch tàu"
                    if detail.get("reason") == "fuzzy":
                        ok_msg = f"{ok_msg} — {detail.get('message')}"
                    await asyncio.to_thread(_report_watchlist_status, "vessel", wl_id, "ok", ok_msg)
```
`vessels.py` `sync_collection_watchlist` — đổi `res = search_vessels(...)` thành:
```python
            detail = search_vessels_detailed(v_site, v_name, v_voyage)
            res = detail.get("items") or []
```
và message ok: `f"{len(res)} schedule(s)" + (f" — {detail['message']}" if detail.get("reason") == "fuzzy" else "")`.

- [ ] **Step 6: Chạy test**

Run: `pytest tests backend/tests -q`
Expected: PASS toàn bộ (kể cả `test_backend_search_vessels_detailed_voyage_mismatch`: "999X" không khớp gần đúng với "1461-012E")

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/eport_client.py backend/app/core/database.py backend/app/services/background_tasks.py backend/app/api/vessels.py tests/test_eport.py
git commit -m "feat(eport): approximate voyage matching flagged as fuzzy

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: Nhãn "Khớp gần đúng" trên giao diện (D2)

**Files:**
- Modify: `frontend/src/types/index.ts:31-52` (`VesselSchedule`)
- Modify: `frontend/src/components/vessel/VesselRow.tsx:63-110`, `frontend/src/components/vessel/VesselDetailModal.tsx`
- Modify: `frontend/src/components/booking/QuickVesselSearch.tsx:300-305`
- Create: `frontend/src/components/vessel/FuzzyVoyageBadge.tsx`
- Modify: `frontend/src/i18n/translations.ts` (`vessel`, vi ~L361, en tương ứng)

**Interfaces:**
- Consumes: `requested_voyage` (DB), `VOYAGE_MATCH`/`REQUESTED_VOYAGE` (kết quả ePort), `match` trong response `/vessels/search` (Task 5, 7)
- Produces: `<FuzzyVoyageBadge requested={string} />` — render `null` khi `requested` rỗng.

- [ ] **Step 1: Kiểu + chuỗi + component**

`types/index.ts` trong `VesselSchedule`: `requested_voyage?: string | null;`

`translations.ts` — `vi.vessel`: `fuzzyMatch: "Gần đúng"`, `fuzzyMatchTooltip: "Khớp gần đúng — số chuyến yêu cầu: {requested}. Hãy kiểm tra lại số chuyến."`; `en.vessel`: `fuzzyMatch: "Approx."`, `fuzzyMatchTooltip: "Approximate match — requested voyage: {requested}. Please double-check the voyage."`

`FuzzyVoyageBadge.tsx`:
```tsx
import React from 'react';
import { useApp } from '../../context/AppContext';
import { tf } from '../../services/i18nFormat';

export const FuzzyVoyageBadge: React.FC<{ requested?: string | null }> = React.memo(({ requested }) => {
  const { t } = useApp();
  if (!requested) return null;
  return (
    <span
      title={tf(t.vessel.fuzzyMatchTooltip, { requested })}
      className="shrink-0 px-1 py-px rounded border border-amber-300 dark:border-amber-700 bg-amber-50 dark:bg-amber-950/60 text-[10px] font-bold text-amber-700 dark:text-amber-300"
    >
      ≈ {t.vessel.fuzzyMatch}
    </span>
  );
});
```

- [ ] **Step 2: Gắn vào bảng, chi tiết và tra nhanh**

`VesselRow.tsx` — thêm nhánh trước `return` mặc định trong `visibleColumns.map`:
```tsx
        if (col.key === 'in_out_voyage' && item.requested_voyage) {
          return (
            <td key={col.key} style={style} className="py-1.5 px-2.5 truncate" title={`${col.label}: ${String(val || '')}`}>
              <div className="flex items-center gap-1.5 truncate">
                <ValueBadge table="vessel" columnKey={col.key} value={val} fallbackText="null" />
                <FuzzyVoyageBadge requested={item.requested_voyage} />
              </div>
            </td>
          );
        }
```
Kiểm tra comparator của `React.memo(VesselRow)` (L156): nếu so sánh từng field thì thêm `requested_voyage`; nếu so `item` theo tham chiếu thì không cần.

`VesselDetailModal.tsx` — cạnh chỗ hiển thị `in_out_voyage`, thêm `<FuzzyVoyageBadge requested={schedule.requested_voyage} />` (dùng đúng tên biến prop của modal).

`QuickVesselSearch.tsx` — sau span voyage (L302): `{row.VOYAGE_MATCH === 'fuzzy' && <FuzzyVoyageBadge requested={row.REQUESTED_VOYAGE} />}`. Thông điệp khung xanh (L275) đã hiển thị `res.message` khi có kết quả? Hiện là `tf(qv.found, …)`; đổi `setMessage` (L138) thành:
```tsx
      setMessage(items.length > 0
        ? [tf(qv.found, { count: items.length }), res?.match === 'fuzzy' ? res?.message : ''].filter(Boolean).join(' — ')
        : res?.message || qv.notFound);
```
Tương tự trong `VesselTab` sau truy vấn ePort (L300-323): khi `res.match === 'fuzzy'` hiện thêm `addToast(res.message, 'info')`.

- [ ] **Step 3: Build + thủ công**

Run: `cd frontend && npm run build`
Thủ công: tra tàu với voyage thiếu số 0 / thiếu hậu tố (ví dụ tàu thật có "0272S" trên ePort, nhập "272") → toast/thông điệp "Đã khớp gần đúng…", dòng trong tab Tàu có nhãn "≈ Gần đúng" với tooltip số chuyến yêu cầu. Tra lại với voyage chính xác → nhãn biến mất.
Expected: đúng.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/types/index.ts frontend/src/components/vessel frontend/src/components/booking/QuickVesselSearch.tsx frontend/src/i18n/translations.ts
git commit -m "feat(vessel-ui): show an approximate-voyage badge with the requested voyage

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: Spike so sánh OCR offline (D1 — tối đa 0.5 ngày) 👤 cần máy Windows + 10 ảnh thật

**Files:**
- Create: `scripts/ocr_spike/compare_ocr.py`
- Create: `scripts/ocr_spike/requirements-spike.txt`
- Create: `scripts/ocr_spike/README.md`
- Modify: `.gitignore` (thêm `scripts/ocr_spike/samples/`, `scripts/ocr_spike/results/`)
- Create: `backend/app/services/ocr_layout.py`, `tests/test_ocr_layout.py`
- Create: `docs/superpowers/plans/2026-09-xx-ocr-spike-results.md` (kết quả)

**Interfaces:**
- Produces: `ocr_layout.boxes_to_text(boxes: list[tuple[list[list[float]], str]]) -> str` — box là 4 điểm `[[x,y],...]`; nhóm theo tâm dọc (dung sai = 0.5 × chiều cao box trung vị), mỗi dòng sắp theo x, nối bằng một dấu cách, các dòng nối bằng `\n`.
- Produces: `python scripts/ocr_spike/compare_ocr.py --samples scripts/ocr_spike/samples` in bảng: engine × (số ô đúng /100, giây/ảnh trung bình, thời gian khởi tạo, dung lượng package).
- Định dạng mẫu: `samples/<tên>.png|jpg` + `samples/<tên>.json` chứa đáp án 10 trường chính (chuỗi đã chuẩn hoá như app xuất ra, trường không có để `""`).

- [ ] **Step 1: Test thất bại cho ghép dòng**

`tests/test_ocr_layout.py`:
```python
from backend.app.services.ocr_layout import boxes_to_text


def box(x, y, w=60, h=20):
    return [[x, y], [x + w, y], [x + w, y + h], [x, y + h]]


def test_boxes_grouped_into_lines_left_to_right():
    boxes = [
        (box(200, 12), "SGN601175800"),
        (box(10, 10), "Booking No:"),
        (box(10, 50), "Vessel:"),
        (box(120, 53), "KOTA NEKAD 0272S"),
    ]
    assert boxes_to_text(boxes) == "Booking No: SGN601175800\nVessel: KOTA NEKAD 0272S"


def test_empty():
    assert boxes_to_text([]) == ""
```

Run: `pytest tests/test_ocr_layout.py -v` → Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 2: Cài đặt**

`backend/app/services/ocr_layout.py`:
```python
from statistics import median
from typing import List, Sequence, Tuple

Box = Sequence[Sequence[float]]


def boxes_to_text(boxes: List[Tuple[Box, str]]) -> str:
    """Rebuild reading order (lines top-to-bottom, words left-to-right) from OCR word/segment boxes,
    so the line-based booking regexes in extractor.py keep working."""
    items = []
    for pts, text in boxes:
        text = (text or "").strip()
        if not text or not pts:
            continue
        ys = [p[1] for p in pts]
        xs = [p[0] for p in pts]
        items.append({"cy": (min(ys) + max(ys)) / 2, "h": max(ys) - min(ys), "x": min(xs), "text": text})
    if not items:
        return ""
    tolerance = max(1.0, median(i["h"] for i in items) * 0.5)
    lines: List[dict] = []
    for it in sorted(items, key=lambda i: i["cy"]):
        if lines and abs(it["cy"] - lines[-1]["cy"]) <= tolerance:
            line = lines[-1]
            line["items"].append(it)
            line["cy"] = sum(i["cy"] for i in line["items"]) / len(line["items"])
        else:
            lines.append({"cy": it["cy"], "items": [it]})
    return "\n".join(" ".join(i["text"] for i in sorted(l["items"], key=lambda i: i["x"])) for l in lines)
```

Run: `pytest tests/test_ocr_layout.py -v` → Expected: PASS

- [ ] **Step 3: Script so sánh**

`scripts/ocr_spike/requirements-spike.txt`:
```
rapidocr-onnxruntime
paddleocr
paddlepaddle
winrt-runtime; sys_platform == "win32"
winrt-Windows.Media.Ocr; sys_platform == "win32"
winrt-Windows.Graphics.Imaging; sys_platform == "win32"
winrt-Windows.Storage.Streams; sys_platform == "win32"
winrt-Windows.Foundation; sys_platform == "win32"
winrt-Windows.Foundation.Collections; sys_platform == "win32"
```

`scripts/ocr_spike/compare_ocr.py`:
```python
"""OCR spike: accuracy of rapidocr / paddleocr / Windows OCR on real booking images (see README.md)."""
import argparse
import asyncio
import json
import os
import sys
import time
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
os.environ.setdefault("DB_PATH", str(Path(__file__).parent / "results" / "spike.db"))

from backend.app.services.extractor import extract_booking_from_text  # noqa: E402
from backend.app.services.ocr_layout import boxes_to_text  # noqa: E402

KEY_FIELDS = ["Booking No", "Carrier", "Vessel", "ETD", "Port of Discharging", "Equipment Type", "Q'ty",
              "Empty Pick Up CY", "Full return CY", "Port Cargo Cut-off"]


def norm(v) -> str:
    v = "" if v in (None, "null") else str(v)
    return " ".join(v.upper().split())


# --- engines: each returns (init_fn, run_fn(image_bytes, path) -> text) -------------------------
def rapidocr_engine():
    from rapidocr_onnxruntime import RapidOCR
    engine = RapidOCR()

    def run(image_bytes, path):
        result, _ = engine(image_bytes)
        return boxes_to_text([(r[0], r[1]) for r in (result or [])])
    return run


def paddleocr_engine():
    from paddleocr import PaddleOCR
    ocr = PaddleOCR(lang="en")

    def run(image_bytes, path):
        if hasattr(ocr, "predict"):  # PaddleOCR 3.x
            res = ocr.predict(str(path))[0]
            return boxes_to_text(list(zip(res["rec_polys"], res["rec_texts"])))
        res = ocr.ocr(str(path)) or [[]]  # PaddleOCR 2.x
        return boxes_to_text([(line[0], line[1][0]) for line in (res[0] or [])])
    return run


def windows_ocr_engine():
    from winrt.windows.graphics.imaging import BitmapDecoder
    from winrt.windows.media.ocr import OcrEngine
    from winrt.windows.storage.streams import DataWriter, InMemoryRandomAccessStream

    engine = OcrEngine.try_create_from_user_profile_languages()
    if engine is None:
        raise RuntimeError("No Windows OCR language pack installed (Settings > Language > add English)")

    async def recognize(image_bytes):
        stream = InMemoryRandomAccessStream()
        writer = DataWriter(stream)
        writer.write_bytes(image_bytes)
        await writer.store_async()
        writer.detach_stream()
        stream.seek(0)
        decoder = await BitmapDecoder.create_async(stream)
        bitmap = await decoder.get_software_bitmap_async()
        result = await engine.recognize_async(bitmap)
        return "\n".join(line.text for line in result.lines)

    def run(image_bytes, path):
        return asyncio.run(recognize(image_bytes))
    return run


ENGINES = {"rapidocr": rapidocr_engine, "paddleocr": paddleocr_engine, "windows": windows_ocr_engine}


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--samples", required=True)
    parser.add_argument("--engines", default="rapidocr,paddleocr,windows")
    args = parser.parse_args()
    samples = sorted(p for p in Path(args.samples).iterdir() if p.suffix.lower() in (".png", ".jpg", ".jpeg"))
    out_dir = Path(__file__).parent / "results"
    out_dir.mkdir(exist_ok=True)
    summary = []
    for name in args.engines.split(","):
        try:
            started = time.perf_counter()
            run = ENGINES[name]()
            init_s = time.perf_counter() - started
        except Exception as e:
            print(f"[{name}] unavailable: {e}")
            continue
        correct, total, seconds = 0, 0, []
        for img in samples:
            expected = json.loads(img.with_suffix(".json").read_text(encoding="utf-8"))
            data = img.read_bytes()
            t0 = time.perf_counter()
            text = run(data, img)
            seconds.append(time.perf_counter() - t0)
            got = extract_booking_from_text(text, img.name)
            (out_dir / f"{name}__{img.stem}.txt").write_text(text, encoding="utf-8")
            for field in KEY_FIELDS:
                total += 1
                ok = norm(got.get(field)) == norm(expected.get(field))
                correct += ok
                if not ok:
                    print(f"[{name}] {img.name} {field}: expected={expected.get(field)!r} got={got.get(field)!r}")
        summary.append((name, correct, total, sum(seconds) / max(1, len(seconds)), init_s))
    print("\nengine      correct/total   avg s/img   init s")
    for name, correct, total, avg, init_s in summary:
        print(f"{name:<11} {correct:>3}/{total:<3} ({correct / max(1, total):.0%})   {avg:8.2f}   {init_s:6.2f}")


if __name__ == "__main__":
    main()
```

`scripts/ocr_spike/README.md` — nội dung:
```markdown
# OCR spike (≤ 0.5 ngày)
1. Chép 10 ảnh booking thật (PIL, CUL, ONE, Dongjin… đủ hãng khách dùng) vào `samples/` — thư mục bị gitignore, KHÔNG commit.
2. Với mỗi ảnh tạo `samples/<tên>.json` chứa 10 trường: Booking No, Carrier, Vessel, ETD, Port of Discharging,
   Equipment Type, Q'ty, Empty Pick Up CY, Full return CY, Port Cargo Cut-off (định dạng như app hiển thị, thiếu thì "").
3. Trên máy Windows sạch (Python 3.11): `pip install -r requirements.txt -r scripts/ocr_spike/requirements-spike.txt`
4. `python scripts/ocr_spike/compare_ocr.py --samples scripts/ocr_spike/samples`
5. Đo dung lượng: `pip show -f rapidocr-onnxruntime onnxruntime` / `paddleocr paddlepaddle` (cộng size thư mục site-packages).
6. Ghi bảng kết quả + lựa chọn vào `docs/superpowers/plans/2026-09-xx-ocr-spike-results.md`.
Tiêu chí chọn (theo thứ tự): đạt ≥ 90/100 → dung lượng bộ cài tăng thêm nhỏ nhất → thời gian đọc/ảnh.
Nếu Windows OCR API đạt: không tăng dung lượng nhưng chỉ chạy Windows 10+ có gói ngôn ngữ; macOS vẫn dùng swift.
Nếu không engine nào đạt 90: xem các dòng sai được in ra — sai do nhận chữ hay do regex trong `extractor.py`; sửa regex là việc riêng trước khi tích hợp.
```

- [ ] **Step 4: Chạy spike (👤 Windows)** — theo README. Kết quả mong đợi: bảng in ra 3 engine. Ghi vào file kết quả kèm quyết định. **Dừng lại báo anh/chị** nếu (a) không engine nào ≥ 90/100, hoặc (b) engine được chọn làm bộ cài tăng > 100MB.

- [ ] **Step 5: Commit**

```bash
git add scripts/ocr_spike/compare_ocr.py scripts/ocr_spike/requirements-spike.txt scripts/ocr_spike/README.md .gitignore backend/app/services/ocr_layout.py tests/test_ocr_layout.py docs/superpowers/plans/2026-09-xx-ocr-spike-results.md
git commit -m "chore(ocr): spike harness comparing rapidocr, paddleocr and Windows OCR

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 10: Tích hợp OCR offline vào app (D1)

> Viết theo phương án dự kiến **rapidocr**. Nếu spike chọn Windows OCR: giữ nguyên cấu trúc (engine `"windows"` trong `available_ocr_engines`, hàm `_ocr_windows` dùng `recognize` từ `compare_ocr.py`, điều kiện `sys.platform == "win32"` và import được `winrt.windows.media.ocr`), thay dependency/spec tương ứng. Nếu chọn paddleocr: như rapidocr với hàm `run` của paddle.

**Files:**
- Modify: `backend/app/services/image_extractor.py:62,288-365`
- Modify: `requirements.txt`, `backend/requirements.txt`, `backend_app.spec`
- Test: `tests/test_image_extractor.py`

**Interfaces:**
- Consumes: `boxes_to_text` (Task 9)
- Produces: engine `"rapidocr"` đứng đầu `available_ocr_engines()` khi `importlib.util.find_spec("rapidocr_onnxruntime")` khác None; `_ocr_rapid(image_bytes: bytes, timeout: float) -> str`; `_get_rapid_engine()` singleton tạo lười, an toàn luồng.

- [ ] **Step 1: Viết test thất bại** — thêm vào `tests/test_image_extractor.py`:
```python
import importlib.util
import threading


def test_available_ocr_engines_prefers_rapidocr(monkeypatch):
    monkeypatch.setattr(ie.importlib.util, "find_spec", lambda name: object() if name == "rapidocr_onnxruntime" else None)
    monkeypatch.setattr(ie.shutil, "which", lambda cmd: None)
    assert ie.available_ocr_engines()[0] == "rapidocr"


def test_run_local_ocr_uses_rapidocr_layout(monkeypatch):
    monkeypatch.setattr(ie, "available_ocr_engines", lambda: ["rapidocr"])

    class FakeEngine:
        def __call__(self, image_bytes):
            return ([[[[0, 50], [80, 50], [80, 70], [0, 70]], "Vessel: KOTA NEKAD 0272S", 0.9],
                     [[[0, 10], [80, 10], [80, 30], [0, 30]], "Booking No: SGN601175800", 0.9]], 0.1)

    monkeypatch.setattr(ie, "_get_rapid_engine", lambda: FakeEngine())
    text, tried = ie.run_local_ocr(b"img")
    assert tried == ["rapidocr"]
    assert text == "Booking No: SGN601175800\nVessel: KOTA NEKAD 0272S"


def test_rapid_engine_created_once(monkeypatch):
    created = []

    class FakeRapid:
        def __init__(self):
            created.append(1)

    fake_module = type(sys)("rapidocr_onnxruntime")
    fake_module.RapidOCR = FakeRapid
    monkeypatch.setitem(sys.modules, "rapidocr_onnxruntime", fake_module)
    monkeypatch.setattr(ie, "_rapid_engine", None)
    threads = [threading.Thread(target=ie._get_rapid_engine) for _ in range(5)]
    [t.start() for t in threads]
    [t.join() for t in threads]
    assert len(created) == 1


@pytest.mark.skipif(importlib.util.find_spec("rapidocr_onnxruntime") is None, reason="rapidocr not installed")
def test_rapidocr_reads_rendered_booking_text():
    from PIL import Image, ImageDraw, ImageFont
    img = Image.new("RGB", (900, 200), "white")
    draw = ImageDraw.Draw(img)
    font = ImageFont.load_default(size=28)
    draw.text((20, 30), "Booking No: SGN601175800", fill="black", font=font)
    draw.text((20, 110), "Vessel: KOTA NEKAD 0272S", fill="black", font=font)
    buf = io.BytesIO()
    img.save(buf, format="PNG")
    text, tried = ie.run_local_ocr(buf.getvalue(), total_timeout=60)
    assert tried and tried[0] == "rapidocr"
    assert "SGN601175800" in text.replace(" ", "")
```
(đảm bảo đầu file có `import io, sys, pytest`.)

- [ ] **Step 2: Chạy, xác nhận thất bại**

Run: `pytest tests/test_image_extractor.py -k "rapid" -v`
Expected: FAIL — `AttributeError: ... has no attribute '_get_rapid_engine'`

- [ ] **Step 3: Cài đặt** — `image_extractor.py`:

Import đầu file thêm `import importlib.util`, `import threading`, `from backend.app.services.ocr_layout import boxes_to_text`.

Dòng 62: `W_NO_KEY = "Chưa cấu hình Gemini API key — đang dùng OCR offline."`

Thêm trước `available_ocr_engines`:
```python
_rapid_engine = None
_rapid_lock = threading.Lock()


def _get_rapid_engine():
    """Load the ONNX models once, on first use (keeps backend startup fast)."""
    global _rapid_engine
    if _rapid_engine is None:
        with _rapid_lock:
            if _rapid_engine is None:
                from rapidocr_onnxruntime import RapidOCR
                _rapid_engine = RapidOCR()
    return _rapid_engine


def _ocr_rapid(image_bytes: bytes, timeout: float) -> str:
    # rapidocr has no timeout knob; runs in-process (typically 2-4s). `timeout` kept for engine signature parity.
    try:
        result, _ = _get_rapid_engine()(image_bytes)
        return boxes_to_text([(r[0], r[1]) for r in (result or [])])
    except Exception as e:
        print(f"RapidOCR failed: {e}")
        return ""
```
`available_ocr_engines()` — đầu hàm:
```python
    engines = []
    if importlib.util.find_spec("rapidocr_onnxruntime") is not None:
        engines.append("rapidocr")
```
`run_local_ocr` — trong vòng lặp thêm nhánh:
```python
        if engine == "rapidocr":
            text = _ocr_rapid(image_bytes, remaining)
        elif engine == "swift":
```
Cập nhật docstring `extract_booking_from_image_detailed`: "Falls back to local OCR (RapidOCR, then macOS Vision via swift / pytesseract) + text parser."

Làm nóng model trong nền để lần đọc ảnh đầu không chờ nạp model: trong `backend/app/main.py` lifespan, sau `restore_auto_sync`, thêm
```python
    asyncio.get_running_loop().run_in_executor(None, _warm_ocr)
```
với
```python
def _warm_ocr():
    try:
        from backend.app.services.image_extractor import available_ocr_engines, _get_rapid_engine
        if "rapidocr" in available_ocr_engines():
            _get_rapid_engine()
    except Exception:
        logging.getLogger("backend").exception("OCR warm-up failed")
```
(đặt `_warm_ocr` ở mức module trong `main.py`; kiểm tra `asyncio`/`logging` đã import.)

- [ ] **Step 4: Dependency + đóng gói**

`requirements.txt` và `backend/requirements.txt` thêm (phiên bản chốt theo spike, dòng dưới là mặc định):
```
rapidocr-onnxruntime==1.4.4
```
`backend_app.spec` — trong vòng `for _collect in (...)` thêm `lambda: collect_data_files('rapidocr_onnxruntime')` (model `.onnx` + `config.yaml`); `hiddenimports` thêm:
```python
        'rapidocr_onnxruntime',
        'onnxruntime',
        'backend.app.services.ocr_layout',
```
và `binaries` bổ sung DLL onnxruntime nếu PyInstaller bỏ sót: trước `a = Analysis(` thêm
```python
from PyInstaller.utils.hooks import collect_dynamic_libs
extra_binaries = []
try:
    extra_binaries += collect_dynamic_libs('onnxruntime')
except Exception as _e:
    print(f'WARNING: onnxruntime binaries not collected: {_e}')
```
và `binaries=extra_binaries,`.

- [ ] **Step 5: Chạy test**

Run: `pip install -r requirements.txt && pytest tests backend/tests -q`
Expected: PASS toàn bộ; `test_rapidocr_reads_rendered_booking_text` chạy (không skip).

- [ ] **Step 6: Đo khởi động + dung lượng (mac)**

Run: `pyinstaller backend_app.spec --noconfirm --clean && du -sh dist/backend_app`
So với build trước (ghi số trước/sau). Chạy `dist/backend_app/backend_app` và đo thời gian tới khi `curl http://127.0.0.1:8000/health` trả 200.
Expected: tăng ≤ 100MB; thời gian sẵn sàng chênh < 1s.

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/image_extractor.py backend/app/main.py requirements.txt backend/requirements.txt backend_app.spec tests/test_image_extractor.py
git commit -m "feat(ocr): bundled offline RapidOCR engine so images read without a Gemini key

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 11: Nghiệm thu OCR trên Windows không có key (D1) 👤

**Files:**
- Modify: `docs/superpowers/plans/2026-09-xx-ocr-spike-results.md` (thêm mục nghiệm thu)

- [ ] **Step 1: Build Windows** — đẩy nhánh để CI build (`.github/workflows/build.yml`) hoặc chạy `npm run build:win` trên máy Windows; cài bộ cài lên máy Windows sạch, **xoá Gemini key** trong Cài đặt.

- [ ] **Step 2: Đọc 10 ảnh** — dán lần lượt 10 ảnh trong `samples/` vào app (Cmd/Ctrl+V). Với mỗi ảnh ghi 10 trường chính đúng/sai so với file `.json` đáp án.
Expected: ≥ 90/100 ô đúng; modal hiện cảnh báo "Chưa cấu hình Gemini API key — đang dùng OCR offline."; `engine_used = "ocr"` (tab Network); mỗi ảnh ≤ 15s kể cả lần đầu.

- [ ] **Step 3: Kiểm tra phụ**
- `backend.log` không có `RapidOCR failed`/`onnxruntime` DLL error.
- Thoát app → Task Manager không còn `backend_app.exe`.
- Bộ cài tăng ≤ 100MB so với v2.0.1.

- [ ] **Step 4: Ghi kết quả + commit**

```bash
git add docs/superpowers/plans/2026-09-xx-ocr-spike-results.md
git commit -m "docs(ocr): Windows no-key acceptance results for offline OCR

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```
