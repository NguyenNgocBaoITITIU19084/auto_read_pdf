c8bc6ac refactor: use pandas reindex for excel column filtering
4f8d779 fix: resolve excel exporter logic and test issues
f07aae7 feat: implement Excel export logic

--- STAT ---

 src/exporter.py        | 11 ++++++++++
 tests/test_exporter.py | 55 ++++++++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 66 insertions(+)

--- DIFF ---

diff --git a/src/exporter.py b/src/exporter.py
new file mode 100644
index 0000000..f30e4b7
--- /dev/null
+++ b/src/exporter.py
@@ -0,0 +1,11 @@
+import pandas as pd
+
+def export_to_excel(data: list[dict], output_path: str, selected_columns: list[str]) -> bool:
+    if not data or not selected_columns:
+        return False
+        
+    df = pd.DataFrame(data)
+    
+    df_filtered = df.reindex(columns=selected_columns).fillna("")
+    df_filtered.to_excel(output_path, index=False)
+    return True
diff --git a/tests/test_exporter.py b/tests/test_exporter.py
new file mode 100644
index 0000000..cfd003f
--- /dev/null
+++ b/tests/test_exporter.py
@@ -0,0 +1,55 @@
+import os
+import pytest
+import pandas as pd
+from src.exporter import export_to_excel
+
+def test_export_to_excel(tmp_path):
+    data = [{"Booking No": "123", "Pre Carrier": "Ship A"}, {"Booking No": "456", "Pre Carrier": "Ship B"}]
+    cols = ["Booking No", "Pre Carrier"]
+    output_file = tmp_path / "test.xlsx"
+    
+    result = export_to_excel(data, str(output_file), cols)
+    
+    assert result is True
+    assert output_file.exists()
+    df = pd.read_excel(output_file, dtype=str)
+    assert len(df) == 2
+    assert list(df.columns) == cols
+
+def test_export_to_excel_early_exit(tmp_path):
+    output_file = tmp_path / "test.xlsx"
+    # No data
+    assert export_to_excel([], str(output_file), ["Col"]) is False
+    assert not output_file.exists()
+    
+    # No selected columns
+    assert export_to_excel([{"Col": "val"}], str(output_file), []) is False
+    assert not output_file.exists()
+
+def test_export_to_excel_missing_fields(tmp_path):
+    data = [
+        {"Booking No": "123"},
+        {"Booking No": "456", "Pre Carrier": None}
+    ]
+    cols = ["Booking No", "Pre Carrier", "Missing Col"]
+    output_file = tmp_path / "test_missing.xlsx"
+    
+    result = export_to_excel(data, str(output_file), cols)
+    
+    assert result is True
+    assert output_file.exists()
+    
+    # keep_default_na=False ensures empty cells are read as "" rather than NaN
+    df = pd.read_excel(output_file, keep_default_na=False, dtype=str)
+    assert list(df.columns) == cols
+    assert len(df) == 2
+    
+    # Check row 1
+    assert df.iloc[0]["Booking No"] == "123"
+    assert df.iloc[0]["Pre Carrier"] == ""
+    assert df.iloc[0]["Missing Col"] == ""
+    
+    # Check row 2
+    assert df.iloc[1]["Booking No"] == "456"
+    assert df.iloc[1]["Pre Carrier"] == ""
+    assert df.iloc[1]["Missing Col"] == ""
