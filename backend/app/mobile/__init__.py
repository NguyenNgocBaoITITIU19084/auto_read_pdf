"""LAN-only FastAPI app for the phone-camera-QR feature (pairing + upload).

Not mounted into the main app and not bound to any network port here — see
the plan's Task 4 for the companion uvicorn server that serves ``mobile_app``
on the LAN.
"""
